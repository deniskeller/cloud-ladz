import { Default } from '@layouts/index';
import { Cryptocurrencies } from '@view/index';

const CryptocurrenciesPage = () => {
	return (
		<Default type='light'>
			<Cryptocurrencies />
		</Default>
	);
};

export default CryptocurrenciesPage;

import { Default } from '@layouts/index';
import { TermsAndConditions } from '@view/index';

const TermsAndConditionsPage = () => {
	return (
		<Default type='light'>
			<TermsAndConditions />
		</Default>
	);
};

export default TermsAndConditionsPage;

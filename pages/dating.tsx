import { Default } from '@layouts/index';
import { Dating } from '@view/index';

const DatingPage = () => {
	return (
		<Default type='light'>
			<Dating />
		</Default>
	);
};

export default DatingPage;

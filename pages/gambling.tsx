import { Default } from '@layouts/index';
import { Gambling } from '@view/index';

const GamblingPage = () => {
	return (
		<Default type='light'>
			<Gambling />
		</Default>
	);
};

export default GamblingPage;

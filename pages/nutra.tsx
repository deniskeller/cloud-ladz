import { Default } from '@layouts/index';
import { Nutra } from '@view/index';

const NutraPage = () => {
	return (
		<Default type='light'>
			<Nutra />
		</Default>
	);
};

export default NutraPage;
